spring.datasource.url=jdbc:postgresql://127.0.0.1:5432/postgres?user=postgres&password=kyt32014

spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL9Dialect

spring.jpa.open-in-view=true

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=update

#Exibe todos endpoints do actuator
management.endpoints.beans.enabled=false
management.endpoints.web.exposure.include=metrics,health

api.jwt-secret=secret
api.jwt-expiration=60000

api.allow-origin=http://localhost:4200